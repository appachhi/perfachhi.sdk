<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.appachhi.sdk">

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- To store the heap dumps and leak analysis results. -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application>
        <service android:name=".OverlayService" />

        <service android:name=".monitor.memoryleak.AppachhiMemoryLeakService" />

        <activity
            android:name=".ui.ConfigurationActivity"
            android:excludeFromRecents="true"
            android:launchMode="singleTop"
            android:theme="@style/TransparentActivity" />

        <service
            android:name="com.squareup.leakcanary.internal.HeapAnalyzerService"
            android:enabled="false"
            android:process=":leakcanary" />
        <service
            android:name="com.squareup.leakcanary.DisplayLeakService"
            android:enabled="false"
            android:process=":leakcanary" />

        <provider
            android:name="com.squareup.leakcanary.internal.LeakCanaryFileProvider"
            android:authorities="com.squareup.leakcanary.fileprovider.${applicationId}"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/leak_canary_file_paths" />
        </provider>

        <activity
            android:name="com.squareup.leakcanary.internal.DisplayLeakActivity"
            android:enabled="false"
            android:icon="@drawable/ic_monitor"
            android:label="@string/leak_canary_display_activity_label"
            android:process=":leakcanary"
            android:taskAffinity="com.squareup.leakcanary.${applicationId}"
            android:theme="@style/leak_canary_LeakCanary.Base"
            tools:replace="android:icon"/>
        <activity
            android:name="com.squareup.leakcanary.internal.RequestStoragePermissionActivity"
            android:enabled="false"
            android:excludeFromRecents="true"
            android:icon="@drawable/ic_monitor"
            android:label="@string/leak_canary_storage_permission_activity_label"
            android:process=":leakcanary"
            android:roundIcon="@drawable/ic_monitor"
            android:taskAffinity="com.squareup.leakcanary.${applicationId}"
            android:theme="@style/leak_canary_Theme.Transparent"
            tools:replace="android:icon,android:roundIcon" />

        <provider
            android:name=".providers.AppachhiInitializer"
            android:authorities="com.appachhi.sdk.initializer.${applicationId}"
            android:exported="false" />
        <service
            android:name=".bugasura.BugaSuraDumpService"
            android:exported="true"
            android:enabled="true"
            android:permission="com.appachhi.bugasura.${applicationId}" >
            <intent-filter>
                <action android:name="com.appachhi.bugasuradump.${applicationId}.ACTION_DUMP"/>
            </intent-filter>
        </service>
    </application>
</manifest>
